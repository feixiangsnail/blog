//数据库四大语句
//DELETE  DELETE FROM 'TABLENAME' WHERE 'TIAOJIAN'
//INSERT INSERT INTO 'TABNAME'(字段列表) VALUES(值列表) 
//UPDATE UPDATE 表 SET 字段=值，字段=值 WHERE 条件
//SELECT SELECT * FROM 表 WHERE 条件

// WHERE age>=18 AND score<60;
// WHERE cath>=18 OR score<60;

// ORDER BY price ASC/DESC;
// ASC 升序 desc 降序
//ORDER BY price ASC,sales DESC;

//GROUP BY 聚类-合并相同
//COUNT MIN MAX AVG
//SELECT *,COUNT(class) FROM `student_table` WHERE score>60 GROUP BY class ORDER BY COUNT(class);






